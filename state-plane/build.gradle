// State Plane module - Hot state, Event log, Snapshot & cursor
// Depends on schema for event models

dependencies {
    implementation project(':schema')
    
    // TODO: Add state storage abstraction (no concrete implementation yet)
}